package models;

public class CoordinateUnitBlockMapCache {
	// Acts like a TLB
	// Store the previous mappings of coordinates to the UnitBlocks so we can avoid accessing the Block Object and its arrays, and directly access the UnitBlock object
}
