package tools;

public class CoordinateBlockMapper {
	/*
	 * Primary task is to return the UnitBlock corresponding to a given coordinate set
	 * Makes us of the CoordinateUnitBlockMapCache (TLB like thing) to speed up the look up process
	 * Creates/Destoys Blocks to make bigger/ smaller blocks
	 * Provides methods to allow the timer reccomend which unit blocks to remove
	 * */
}
